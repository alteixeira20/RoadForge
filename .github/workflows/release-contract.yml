name: Release Contract

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'package.json'
      - 'apps/web/package.json'
      - 'packages/roadforge-mcp/package.json'
      - 'apps/api/src/api/config.py'
      - 'CHANGELOG.md'
      - 'Makefile'
      - '.nvmrc'
      - 'tools/check-release-contract.mjs'
      - '.github/workflows/release-contract.yml'
  push:
    branches:
      - main
      - dev
    paths:
      - 'package.json'
      - 'apps/web/package.json'
      - 'packages/roadforge-mcp/package.json'
      - 'apps/api/src/api/config.py'
      - 'CHANGELOG.md'
      - 'Makefile'
      - '.nvmrc'
      - 'tools/check-release-contract.mjs'
      - '.github/workflows/release-contract.yml'

permissions:
  contents: read

concurrency:
  group: release-contract-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Version & Toolchain Contract
    runs-on: ubuntu-24.04
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up pnpm
        uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0
        with:
          run_install: false

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate release invariants
        run: pnpm check:release
