# Public API base URL used by apps/web when backend integration is enabled.
# Keep empty or point to the local backend server during development.
# Copy this file to .env.local and set the value before starting the dev server.
NEXT_PUBLIC_API_URL=http://localhost:7878
